# Example Environment Configuration for cryptodb-c

# PostgreSQL connection string
# Format: "host=<host> dbname=<database> user=<user> password=<password>"
# export DB_CONNINFO="host=localhost dbname=cryptodb user=postgres password=postgres"

# For local development
export DB_CONNINFO="host=localhost dbname=cryptodb user=postgres password=postgres"

# For remote database
# export DB_CONNINFO="host=db.example.com port=5432 dbname=cryptodb user=myuser password=mypassword sslmode=require"

# Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your database credentials
# 3. Source it: source .env
# 4. Run the application: ./cryptodb
